{
  "name": "@codexpulse/relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:migrate": "node --env-file-if-exists=../../.env --import tsx src/migrate.ts",
    "dev": "node --env-file=../../.env --import tsx --watch src/dev-server.ts",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@codexpulse/contracts": "*"
  }
}
